#include <iostream>
using namespace std;
typedef unsigghed int unit;
int main(int argc, char* argv[]) {
	int n = 0;
	std::cin >> n;
	int sumbits = 0;
	for (int = i = 0; < BITS; ++i)
	{
		sumbits += (n >> i) & 1;
		std::cout << sumbits << std::endl;
		return 0}
;
}